# learning-django-rest-recipe

## Useful commands

To run linting:

```bash
docker-compose run --rm app sh -c "flake8"
```

To initialize djano project

```bash
docker-compose run --rm app sh -c "django-admin startproject app ."
```
